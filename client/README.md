# Front (Vite + React)

Instruções gerais, deploy e entrega: veja o **[README na raiz do repositório](../README.md)**.

```bash
npm install
npm run dev
```

Variável: `VITE_API_URL` (URL da API, sem barra no final). Ver `.env.example`.
